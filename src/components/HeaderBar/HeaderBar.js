import React from 'react';

const HeaderBar = ({ })
                <header>{`${config.name} ${config.season} ${this.state.lastUpdated} Serierunde ${
                    this.state.gameweek
                }`}</header>